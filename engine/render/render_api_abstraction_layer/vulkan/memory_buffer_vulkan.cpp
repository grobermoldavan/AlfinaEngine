
#include "memory_buffer_vulkan.h"

namespace al
{
    MemoryBuffer* memory_buffer_vulkan_create(MemoryBufferCreateInfo* createInfo)
    {
        return nullptr;
    }

    void memory_buffer_vulkan_destroy(MemoryBuffer* buffer)
    {

    }
}
